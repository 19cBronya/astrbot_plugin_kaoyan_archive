# Changelog

## v0.7.0

- 题目详情页为已归档题目新增“重新归档”，整理失败的题目也可从同一入口再次提交。
- 重新归档严格复用题目已关联的不可变原始事件；模型成功后自动保存旧派生内容为只追加修订，再更新展示结果。
- 重新归档时若所有 Provider 仍失败，不再用本地兜底覆盖既有结果；页面保留旧内容并显示本次错误，方便 API 恢复后继续重试。

## v0.6.0

- 题目详情页新增“编辑归档”，可修改归档科目、标题、概览、知识点和 Markdown 总结。
- 编辑接口复用 Dashboard 登录鉴权并校验字段长度；修改科目时既有题号保持不变，原始会话不受影响。
- 每次有效修改前自动保存完整旧值至只追加修订表，详情中显示累计修订次数；重复保存相同内容不会产生空修订。

## v0.5.0

- 归档模型新增独立 `overview` 字段，以 1–2 句话概括问题目标、核心结论或解题方向。
- 题库列表在标题下直接展示题目概览，并支持通过概览内容搜索题目。
- SQLite schema 升级至 v3；现有归档题目会从原有总结安全回填概览，不修改原始事件。
- 模型不可用或未返回概览时，由本地规则生成兜底概览，归档流程不会中断。

## v0.4.1

- 题目详情的原始会话会直接内嵌展示 JPEG、PNG、GIF、WebP 和 AVIF 题图，点击可切换原图大小。
- 图片通过已登录的 Plugin Page bridge 按 SHA-256 获取；服务端校验 MIME、大小、内容哈希和附件根目录，拒绝路径穿越及篡改文件。
- 非图片附件继续显示文件信息；图片加载失败时保留明确的错误提示和附件记录。

## v0.4.0

- Plugin Page 使用随插件打包的 KaTeX 0.18.5 离线渲染公式，不依赖公网 CDN。
- 题目总结和原始会话支持 `\\(...\\)`、`\\[...\\]`、`$...$`、`$$...$$` 及常见公式环境。
- 跨行独立公式会在 Markdown 分段前合并渲染；公式过长时可横向滚动，失败时保留原文。
- 归档 Prompt 要求保留公式并统一使用可渲染的 LaTeX 分隔符。

## v0.3.0

- Plugin Page 改为全宽题库，移除页面左侧 UMO 白名单编辑区；UMO 仍由 AstrBot 标准插件配置管理。
- 新增科目、状态、会话下拉筛选及知识点搜索，题目列表可直接进入详情。
- 详情页分区展示结构化知识点、可读 Markdown 总结和完整原始会话时间线。
- 归档模型新增 `knowledge_points` 结构化输出，并自动迁移现有 SQLite 数据库；旧题继续兼容显示。

## v0.2.2

- 新增共享备用 LLM Provider；分类和归档均按“专用模型 → 备用模型 → UMO 当前模型”自动降级。
- 模型返回错误或无效 JSON 都会触发下一级 Provider；全部失败时继续使用本地安全规则，不阻断消息保存或题目归档。

## v0.2.1

- 在 Plugin Page 中检查每个白名单 UMO 所使用的 AstrBot 配置及 `plugin_set`，提示框架级路由是否在消息到达插件前排除了本插件。
- 为白名单消息进入处理器和原始消息持久化增加 INFO 日志，便于区分路由、分类和存储故障。

## v0.2.0

- 每条白名单私聊自然语言消息由 LLM 分类为问题、归档边界或软指令。
- 使用 AstrBot 命令注册机制提供 `/kaoyan` 调试与恢复入口。
