# Changelog

## v0.10.1

- 修复用户在助手回答返回前发送结束边界时，迟到回答无法进入原题的问题；现在按 `parent_event_id` 自动关联。
- 插件初始化时扫描历史迟到回答：补回原题，并将被后续区间误收的回答改为该区间的 `excluded` 审计事件。
- 新增归档重跑请求状态；若回答在整理任务运行期间返回，当前任务完成后会自动再次整理，避免竞态吞掉更新。
- 区间建立时排除已关联其他题目的助手回答，避免同一迟到回答再次被新题收录。

## v0.10.0

- 总览将“归档科目”替换为可点击的“待归档消息”，统计已判定为题目但尚未关联题目的用户消息。
- 异常管理页面新增待归档消息视图，同时展示每条用户消息及其对应助手回答。
- 支持多选同一 UMO 的待归档消息，并选择该会话中一个已有题目，将其作为补充追问和回答并入后自动重新归档。
- 已手动并入旧题的消息会从后续区间归档中排除，避免再次发送“我问完了”时重复形成新题。
- 手动关联及重新归档均写入审计记录，原始事件保持不可变。

## v0.9.0

- 分类专用 Provider、全部备用 Provider 与 UMO 当前 Provider 均失败时，不再把消息默认当作题目；完整原始事件进入待分类队列并排除出题目正文。
- 总览用可点击的“待分类”替代“正在整理”；“待分类”和“整理失败”均进入异常修复中心。
- 异常修复中心支持多选、全选、批量 AI 分类重试、批量重新归档，以及人工标为题目内容、指令/无关消息或结束边界。
- 分类修复保存为只追加修订，自动修正事件关系并重新整理受影响题目；原始事件不更新、不删除。
- 题目详情默认只展示正文、回答和结束边界，已排除事件折叠到独立审计区。

## v0.8.4

- 修复题库概览、知识点和总结中未带分隔符的裸公式无法渲染的问题；已有归档无需重跑。
- 前端只推断含等号、上下标、积分号等明确数学特征的片段，并保留已有规范 LaTeX 不变。
- 归档 Prompt 升级至 `archive-v5`，要求所有字段使用 JSON 更稳定的 `$...$` / `$$...$$` 公式分隔符。

## v0.8.3

- 修复题目详情图片因 `loading=lazy` 与 `display:none` 相互等待而一直停留在“图片加载中”的问题。
- 图片预览请求增加 15 秒超时反馈，接口异常时不再无限显示加载状态。
- 附件持久化格式和内容寻址路径不变，现有已保存图片无需迁移或重新归档。

## v0.8.2

- 修复 AstrBot Plugin Page iframe 禁止原生浏览器模态框时，“重新归档”和“软删除”点击后无响应的问题。
- 改用页面内确认对话框，并在提交期间禁用操作按钮，避免重复请求。
- 后端为成功接收的页面题目操作增加 INFO 日志，便于确认重归档是否已入队。

## v0.8.1

- 备用 LLM 列表接入 AstrBot Dashboard 的现有 Provider 多选器，无需手工输入 Provider ID。
- 选择器支持多选、移除及上下调整顺序，保存顺序即模型降级顺序。

## v0.8.0

- 备用 LLM 配置由单个 Provider 扩展为有序 `fallback_provider_ids` 列表，分类和归档共用该降级链。
- 调用顺序固定为“专用 Provider → 全部备用 Provider → UMO 当前 Provider”，并跳过空值及重复项。
- 继续兼容旧版 `fallback_provider_id` 单值配置，升级后无需立即修改现有配置。

## v0.7.0

- 题目详情页为已归档题目新增“重新归档”，整理失败的题目也可从同一入口再次提交。
- 重新归档严格复用题目已关联的不可变原始事件；模型成功后自动保存旧派生内容为只追加修订，再更新展示结果。
- 重新归档时若所有 Provider 仍失败，不再用本地兜底覆盖既有结果；页面保留旧内容并显示本次错误，方便 API 恢复后继续重试。

## v0.6.0

- 题目详情页新增“编辑归档”，可修改归档科目、标题、概览、知识点和 Markdown 总结。
- 编辑接口复用 Dashboard 登录鉴权并校验字段长度；修改科目时既有题号保持不变，原始会话不受影响。
- 每次有效修改前自动保存完整旧值至只追加修订表，详情中显示累计修订次数；重复保存相同内容不会产生空修订。

## v0.5.0

- 归档模型新增独立 `overview` 字段，以 1–2 句话概括问题目标、核心结论或解题方向。
- 题库列表在标题下直接展示题目概览，并支持通过概览内容搜索题目。
- SQLite schema 升级至 v3；现有归档题目会从原有总结安全回填概览，不修改原始事件。
- 模型不可用或未返回概览时，由本地规则生成兜底概览，归档流程不会中断。

## v0.4.1

- 题目详情的原始会话会直接内嵌展示 JPEG、PNG、GIF、WebP 和 AVIF 题图，点击可切换原图大小。
- 图片通过已登录的 Plugin Page bridge 按 SHA-256 获取；服务端校验 MIME、大小、内容哈希和附件根目录，拒绝路径穿越及篡改文件。
- 非图片附件继续显示文件信息；图片加载失败时保留明确的错误提示和附件记录。

## v0.4.0

- Plugin Page 使用随插件打包的 KaTeX 0.18.5 离线渲染公式，不依赖公网 CDN。
- 题目总结和原始会话支持 `\\(...\\)`、`\\[...\\]`、`$...$`、`$$...$$` 及常见公式环境。
- 跨行独立公式会在 Markdown 分段前合并渲染；公式过长时可横向滚动，失败时保留原文。
- 归档 Prompt 要求保留公式并统一使用可渲染的 LaTeX 分隔符。

## v0.3.0

- Plugin Page 改为全宽题库，移除页面左侧 UMO 白名单编辑区；UMO 仍由 AstrBot 标准插件配置管理。
- 新增科目、状态、会话下拉筛选及知识点搜索，题目列表可直接进入详情。
- 详情页分区展示结构化知识点、可读 Markdown 总结和完整原始会话时间线。
- 归档模型新增 `knowledge_points` 结构化输出，并自动迁移现有 SQLite 数据库；旧题继续兼容显示。

## v0.2.2

- 新增共享备用 LLM Provider；分类和归档均按“专用模型 → 备用模型 → UMO 当前模型”自动降级。
- 模型返回错误或无效 JSON 都会触发下一级 Provider；全部失败时继续使用本地安全规则，不阻断消息保存或题目归档。

## v0.2.1

- 在 Plugin Page 中检查每个白名单 UMO 所使用的 AstrBot 配置及 `plugin_set`，提示框架级路由是否在消息到达插件前排除了本插件。
- 为白名单消息进入处理器和原始消息持久化增加 INFO 日志，便于区分路由、分类和存储故障。

## v0.2.0

- 每条白名单私聊自然语言消息由 LLM 分类为问题、归档边界或软指令。
- 使用 AstrBot 命令注册机制提供 `/kaoyan` 调试与恢复入口。
